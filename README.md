# mail-roundtrip-test-api
A bash-based HTTP API service that monitors email server health by sending test emails via SMTP and verifying delivery via IMAP. Perfect for use with monitoring tools like Uptime Kuma.[^1]
